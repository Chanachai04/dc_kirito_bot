const handleChatCommand = require("./chat");
const handleSearchCommand = require("./search");
const handleMention = require("./mention");
const handleImageCommand = require("./image");

module.exports = {
  handleChatCommand,
  handleSearchCommand,
  handleMention,
  handleImageCommand,
};
